{
  "name": "bult-sender",
  "version": "1.0.0",
  "description": "EVM token bulk sender for up to 1000 addresses",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "deploy": "node scripts/deploy-guide.js",
    "generate-csv": "node scripts/generate-csv.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["ethereum", "token", "bulk", "sender", "evm"],
  "author": "grxkun",
  "license": "MIT",
  "dependencies": {
    "ethers": "^6.7.1",
    "dotenv": "^16.3.1",
    "commander": "^11.0.0",
    "csv-parser": "^3.0.0"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^4.9.3"
  }
}